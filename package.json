{
  "name": "madar-erp-root",
  "version": "1.0.0",
  "private": true,
  "description": "Madar ERP Cloud Monorepo Root",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "build:client": "npm run build --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
